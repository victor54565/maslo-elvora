<?php
/**
 * Конфигурация админ-панели «Дух Лес».
 *
 * ВАЖНО про безопасность:
 * - Пароль хранится ТОЛЬКО в виде bcrypt-хэша. Исходный пароль нигде
 *   не записан и никогда не отправляется в браузер. Даже открыв этот файл
 *   на сервере, восстановить пароль из хэша практически невозможно.
 * - Этот файл — .php, поэтому при прямом запросе по URL сервер его ВЫПОЛНИТ,
 *   а не покажет содержимое. Хэш наружу не утекает.
 *
 * Чтобы сменить пароль — сгенерируйте новый хэш командой:
 *   php -r "echo password_hash('НОВЫЙ_ПАРОЛЬ', PASSWORD_DEFAULT), PHP_EOL;"
 * и вставьте результат в 'password_hash' ниже.
 */

return [
    // Хэш пароля 123451010 (bcrypt).
    'password_hash' => '$2y$10$O7JsXBcpJLirelzKaEmDbumlbCNMJt1ugaEoRDPA57U9FAo2tY3Zy',

    // Пути к данным и загрузкам (файловая система, не URL).
    'data_file'         => __DIR__ . '/../data/videos.json',
    'catalog_data_file' => __DIR__ . '/../data/catalog.json',
    'upload_dir' => __DIR__ . '/../uploads',
    'upload_url' => 'uploads',

    // Лимиты загрузки файлов.
    'max_thumb_bytes' => 5 * 1024 * 1024,     // 5 МБ для превью
    'max_video_bytes' => 200 * 1024 * 1024,   // 200 МБ для mp4
];
